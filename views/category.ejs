<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%=category%> Category</title>
</head>
<body>
    <%- include('./partials/nav') %>
    <h1><%= category %></h1>
        <% menuItems.forEach(item => { %> <!--check server.js for deets-->
            <h3><%= item.name %></h3>
            <p>Price: $<%= item.price %></p>
            <p>Rated <%= item.rating %> out of 5</p>
            <p><%= item.details %></p>
        <% }); %>
</body>
</html>
